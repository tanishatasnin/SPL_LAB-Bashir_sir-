#include <stdio.h>
int main()
{
               // write a c program of ever or odd
               int x;
               scanf("%d", &x);
               // use = when we use to a assign operator
               // and ==  it will be condition checking ,
               if (x % 2 == 0)
               {
                              printf("the number is even");
               }
               // else is by its a condition ,it means all the previous condition are false.
               else
               {
                              printf("the number is odd");
               }
               return 0;
}
